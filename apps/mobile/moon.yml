language: "Node"

tasks:
  start:
    command: "npx expo"
  clean:
    command: "npx expo -c"
  local:
    command: "npx expo start --localhost --android"
  android:
    command: "npx expo start --android"
  ios:
    command: "npx expo start --ios"